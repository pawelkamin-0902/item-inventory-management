version: "3"

rpc:
  listen: "tcp://127.0.0.1:6001"

server:
  command: "php worker.php"
  relay: "pipes"

grpc:
  listen: "tcp://0.0.0.0:9001"
  proto:
    - "protos/item.proto"
  workers:
    command: "php worker.php"
    pool:
      num_workers: 2
